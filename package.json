{
  "name": "claude-mem-sync",
  "version": "1.0.0",
  "description": "Team memory sharing for claude-mem — sync AI memories across developers",
  "type": "module",
  "bin": {
    "mem-sync": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "optionalDependencies": {
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "keywords": ["claude", "claude-mem", "memory", "team", "sync", "ai"],
  "author": "Lorenzo",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lopadova/claude-mem-sync.git"                
  },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  }
}
